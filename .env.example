# Copy this file to .env and fill in your values. NEVER commit .env.
# The token has read access to all 8 SWEN DBs — treat it like a password.

NOTION_TOKEN=ntn_replace_me

# Optional: override defaults at build time
# SITE_URL=https://swen-univaq.github.io
# SITE_BASE=/website

# Optional: GitHub API token for higher rate limits when fetching software repo stats
# GITHUB_TOKEN=
